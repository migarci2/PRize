# Frontend Environment Variables

# Solana Network (localhost | devnet | testnet | mainnet-beta)
NEXT_PUBLIC_SOLANA_NETWORK=devnet

# Optional: Custom RPC endpoint
# NEXT_PUBLIC_RPC_ENDPOINT=https://api.devnet.solana.com
